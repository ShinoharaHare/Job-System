// import { } from 'ts-mongoose'
import { Types } from 'mongoose'
import { Account } from '@/server/models'

export const sendNots = (users: Types.ObjectId[], messege: string) => { // type???
    //                (condition, )
    // Account.updateMany({
    //                 _id: {
    //                     $in: [ "$_id", users]}}, ()=>{

    //                     })
}
